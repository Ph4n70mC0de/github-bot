import type { App } from "@octokit/app";

/**
 * Phase 3 deliverable.
 *
 * Currently unused from Phase 2, but registered in handlers/index.ts
 * so the event router stays wired as we add handler logic incrementally.
 */
export function registerPRHandlers(_app: App) {
  // TODO: implement in Phase 3
}
